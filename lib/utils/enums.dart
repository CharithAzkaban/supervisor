enum EndPointEnum {
  addtasks,
  editprofile,
  login,
  password,
  performers,
  refreshtoken,
  tasktypes,
}

enum ImageEnum {
  error,
  glass,
  logo,
  logosingle,
  profile,
}

enum MethodEnum {
  delete,
  get,
  patch,
  post,
  put,
}

enum PageEnum {
  asign,
  dashboard,
  loading,
  password,
  profile,
  signin,
}

enum PrefEnum {
  accesstoken,
}
