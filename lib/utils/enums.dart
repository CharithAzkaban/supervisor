enum EndPointEnum {
  addtasks,
  login,
  performers,
  refreshtoken,
}

enum ImageEnum {
  error,
  glass,
  logo,
  logosingle,
  profile,
}

enum MethodEnum {
  delete,
  get,
  patch,
  post,
  put,
}

enum PageEnum {
  asign,
  dashboard,
  loading,
  signin,
}

enum PrefEnum {
  accesstoken,
}
